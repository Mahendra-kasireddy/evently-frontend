import { CouponsContainer } from './container';

/** Organizer's own coupons, and what they have cost. */
export function CouponsPage() {
  return <CouponsContainer />;
}

export default CouponsPage;
