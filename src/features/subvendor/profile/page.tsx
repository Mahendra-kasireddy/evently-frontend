import { SubvendorProfileContainer } from './container';

/** Sub-vendor's own profile. */
export function SubvendorProfilePage() {
  return <SubvendorProfileContainer />;
}

export default SubvendorProfilePage;
